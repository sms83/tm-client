import { FindtasknamePipe } from './findtaskname.pipe';

describe('FindtasknamePipe', () => {
  it('create an instance', () => {
    const pipe = new FindtasknamePipe();
    expect(pipe).toBeTruthy();
  });
});
